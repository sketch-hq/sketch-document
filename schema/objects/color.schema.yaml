$schema: http://json-schema.org/draft-07/schema
$id: https://github.com/BohemianCoding/sketch-file-format#color
title: Color
description: Color value encoded as alpha, red, green and blue components.
type: object
required:
  - _class
  - alpha
  - red
  - green
  - blue
properties:
  _class: { const: color }
  alpha: { $ref: ../utils/unit-interval.schema.yaml }
  red: { $ref: ../utils/unit-interval.schema.yaml }
  green: { $ref: ../utils/unit-interval.schema.yaml }
  blue: { $ref: ../utils/unit-interval.schema.yaml }
