title: Gradient Asset
description: Stores a named gradient
type: object
required:
  - _class
  - name
  - color
properties:
  _class: { const: MSImmutableGradientAsset }
  name: { type: string }
  gradient: { $ref: ./gradient.schema.yaml }
