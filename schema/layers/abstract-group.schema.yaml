$schema: http://json-schema.org/draft-07/schema
$id: https://github.com/BohemianCoding/sketch-file-format#abstract-group
title: Abstract Group
description: Abstract base schema containing all properties common to groups.
type: object
allOf:
  - $ref: ./abstract-layer.schema.yaml
  - type: object
    required:
      - layers
      - hasClickThrough
    properties:
      hasClickThrough:
        type: boolean
        description: TODO
      groupLayout:
        type: object
        properties:
          _class: { const: MSImmutableFreeformGroupLayout } # TODO: What is this? Is it still in use?
      layers:
        description: TODO
        type: array
        items: { type: object }
# oneOf: # TODO: Enumerate all layers and groups here
#   - { $ref: ./artboard.schema.yaml }
#   - { $ref: ./group.schema.yaml }
