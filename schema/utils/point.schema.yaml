$schema: http://json-schema.org/draft-07/schema
$id: https://github.com/BohemianCoding/sketch-file-format#point
title: Point
description:
  A string representation of a 2D point, normalised between 0 and 1 in the
  format {x, y}
type: string
pattern: '^{(0\.\d*|1|0), (0\.\d*|1|0)}$'
