title: Hydrated Document
description:
  A document object with its page ref array replaced with an array of full page
  objects.
allOf:
  - $ref: ./abstract-document.schema.yaml
  - type: object
    required:
      - _class
      - pages
    properties:
      _class: { const: document }
      pages:
        type: array
        items: { $ref: ./layers/page.schema.yaml }
